set default-bg rgba(0,0,0,0.6)
set recolor-lightcolor rgba(0,0,0,0)

set selection-clipboard clipboard

#set font "iosevka:medium:italic:size=12"

map w exec "zathuraWC '$FILE'"
map = adjust_window best-fit

set zoom-step 2

#map p exec "lpr '$FILE' -P Bro -o print-quality=5 -o media=a4 -o sides=two-sided-long-edge 

map p exec "/home/mariussw/scripts/printscript '$FILE'"
